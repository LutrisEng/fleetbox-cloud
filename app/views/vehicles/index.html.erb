<p style="color: green"><%= notice %></p>

<h1>Vehicles</h1>

<div id="vehicles" class="card-group">
  <% @vehicles.each do |vehicle| %>
    <div id="<%= dom_id vehicle %>" class="card">
      <div class="card-body">
        <%= render vehicle %>
        <% if policy(vehicle).show? %>
          <%= link_to t('vehicle.link.show'), vehicle_path(vehicle), class: 'card-link' %>
        <% end %>
        <% if policy(vehicle).edit? %>
          <%= link_to t('vehicle.link.edit'), edit_vehicle_path(vehicle), class: 'card-link' %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>

<%= button_to "New vehicle", new_vehicle_path, method: :get, class: 'btn btn-primary my-4' %>
