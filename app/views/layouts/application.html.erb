<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title>FleetboxCloud</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Fleetbox</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% navbar_items.each do |item| %>
              <li class="nav-item">
                <% cls = if current_navbar_item == item[:id]
                           'nav-link active'
                         else
                           'nav-link'
                         end %>
                <% aria_current = if current_navbar_item == item[:id]
                                    'page'
                                  end %>
                <%= link_to t("navbar.items.#{item[:id]}"), item[:href], class: cls, aria: { current: aria_current } %>
              </li>
            <% end %>
          </ul>
          <% if current_user %>
            <ul class="navbar-nav d-flex align-items-center">
              <span class="navbar-text"><%= t("navbar.welcome_logged_in", user_name: current_user.name) %></span>
              <li class="nav-item">
                <%= link_to t("navbar.logout"), logout_path, class: 'btn btn-sm btn-outline-warning mx-2', data: { turbo: false } %>
              </li>
            </ul>
          <% else %>
            <ul class="navbar-nav">
              <li class="nav-item">
                <%= link_to t("navbar.login"), login_path, class: 'nav-link' %>
              </li>
            </ul>
          <% end %>
        </div>
      </div>
    </nav>
    <div class="container">
      <% if notice %>
        <p class="alert alert-secondary"><%= notice %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
